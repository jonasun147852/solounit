{
  "name": "solounit",
  "description": "Private mirrors for agent power-users: emergency diagnosis when a tool fails, plus doctor/wallet/graft commands. Everything runs locally; only the explicit /solounit:sync command touches the network.",
  "version": "0.3.0",
  "author": {
    "name": "SoloUnit"
  }
}
