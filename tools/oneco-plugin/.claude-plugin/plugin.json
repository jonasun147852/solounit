{
  "name": "solounit",
  "description": "Private mirrors for agent power-users: emergency diagnosis when a tool fails, plus doctor/wallet/delivery/graft commands. Everything runs locally; only the explicit /solounit:sync command touches the network.",
  "version": "0.6.0",
  "author": {
    "name": "SoloUnit"
  }
}
